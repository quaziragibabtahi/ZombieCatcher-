# ZombieCatcher-
This project demonstrates OOP concepts such as encapsulation (private fields with getters), abstraction (clear methods hiding logic), and composition (classes built from other classes). Each class has a single responsibility, making the code modular, reusable, and easy to extend.
